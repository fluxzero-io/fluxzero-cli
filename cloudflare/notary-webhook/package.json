{
  "name": "fluxzero-notary-webhook",
  "version": "0.1.0",
  "description": "Cloudflare Worker bridge from Apple notarization webhooks to GitHub Actions",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "node --test"
  },
  "devDependencies": {
    "wrangler": "^4.63.0"
  }
}
