name: Deploy API to Cloudflare

on:
  workflow_run:
    workflows: ["Auto Release and Version Bump"]
    types:
      - completed
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy API to Cloudflare
    runs-on: ubuntu-24.04
    # Only deploy if the release workflow completed successfully
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup tools from .tool-versions
        uses: jdx/mise-action@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Get latest release version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            # For manual deployment, use the latest git tag
            VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "dev")
          else
            # For automatic deployment after release, get version from the release workflow
            VERSION=$(echo "${{ github.event.workflow_run.head_branch }}" | sed 's/.*\///g')
            if [ -z "$VERSION" ] || [ "$VERSION" == "main" ]; then
              VERSION=$(git describe --tags --abbrev=0 2>/dev/null || echo "dev")
            fi
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Deploying version: $VERSION"

      - name: Build API JAR
        run: |
          echo "Building shaded JAR for API..."
          ./gradlew :api:shadowJar -PappVersion=${{ steps.get_version.outputs.version }} --info
          echo "Verifying JAR exists..."
          ls -lh api/build/libs/flux-api-*.jar
          echo "Testing JAR locally..."
          java -jar api/build/libs/flux-api-*.jar &
          sleep 6
          if curl -f http://localhost:8080/api/health; then
            echo "✅ API JAR health check passed"
          else
            echo "⚠️ API JAR health check failed (but continuing deployment)"
          fi
          pkill -f "flux-api" || true

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'npm'
          cache-dependency-path: 'api/package-lock.json'

      - name: Deploy to Cloudflare Containers
        run: |
          cd api
          # Copy the shaded JAR to the expected location for Docker build
          cp build/libs/flux-api-*.jar ./flux-api.jar
          # Install Node.js dependencies for Wrangler
          npm install
          
          # Deploy using Wrangler
          npx wrangler deploy --env production
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}

      - name: Verify deployment
        run: |
          echo "Waiting for deployment to be ready..."
          sleep 30

          # Test health endpoint on Cloudflare
          API_URL="https://fluxzero-cli-api-production.rene7346.workers.dev"
          
          echo "Testing deployed API at: $API_URL"

          # Test health endpoint
          if curl -f "$API_URL/health"; then
            echo "✅ API deployment successful! Health check passed."
            echo "🚀 API is live at: $API_URL"
          else
            echo "❌ API deployment failed! Health check did not pass."
            echo "Note: You may need to update the API_URL in the workflow with your actual worker subdomain"
            exit 1
          fi
